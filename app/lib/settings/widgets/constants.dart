import 'dart:ui';

import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';

const mediumGrayColor = Color(0xFFC7C7CC);
const itemPressedColor = Color(0xFFD9D9D9);
const borderColor = Color(0xFFBCBBC1);
const borderLightColor = Color.fromRGBO(49, 44, 51, 1);
const backgroundGray = Color(0xFFEFEFF4);
const groupSubtitle = Color(0xFF777777);
const iosTileDarkColor = Color.fromRGBO(28, 28, 30, 1);
const iosPressedTileColorDark = Color.fromRGBO(44, 44, 46, 1);
const iosPressedTileColorLight = Color.fromRGBO(230, 229, 235, 1);

const defaultTitlePadding = EdgeInsets.only(
  left: 15,
  right: 15,
  bottom: 6,
);

const defaultCupertinoForwardIcon = Icon(
  CupertinoIcons.forward,
  size: 21,
  color: mediumGrayColor,
);

const defaultCupertinoForwardPadding = EdgeInsetsDirectional.only(
  start: 2.25,
);
