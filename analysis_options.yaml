include: package:lint/analysis_options.yaml

analyzer:
  exclude:
    - lib/generated/intl/**
    - "**/*.g.dart"
    - "**/*.freezed.dart"
    - "dependencies/**"
  errors:
    sort_pub_dependencies: ignore
