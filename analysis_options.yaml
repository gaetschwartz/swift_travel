include: package:lint/analysis_options.yaml

analyzer:
  exclude:
    - lib/generated/intl/**
    - lib/generated/l10n.dart
    - "**/*.g.dart"
    - "**/*.freezed.dart"
  errors:
    sort_pub_dependencies: ignore
  # enable-experiment:
  #   - non-nullable
